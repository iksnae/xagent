# Add a Development section to the README (build + test instructions)

Add a short **Development** section to the README documenting how to build and run the test suite locally (the standard Go commands for this project).

Scope: README-only, small and self-contained. Acceptance:
- A `## Development` section exists in the README.
- It shows how to build (e.g. `go build ./...`) and run tests (e.g. `go test ./...`).
- No code changes.
